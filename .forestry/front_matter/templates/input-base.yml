---
label: Input Select
hide_body: true
fields:
- name: base
  type: include
  config: {}
  template: select
  label: Base
- name: options
  type: list
  config:
    use_select: false
    min: 
    max: 
    source:
      type: simple
  label: Options
